# Progress Tracker

Update this file whenever the current phase, active feature, or implementation state changes.

## Current Phase

- Phase 1: Foundation & Design System

## Current Goal

- Implement the real-time collaborative canvas using Liveblocks and React Flow.

## Completed

- Design System & UI Primitive Components Setup ([01-design-system.md](../context/feature-specs/01-design-system.md))
- Base Editor Chrome Layout Components ([02-editor.md](../context/feature-specs/02-editor.md))
- Clerk Authentication & Route Protection Setup ([03-auth.md](../context/feature-specs/03-auth.md))
- Project Dialogs & Editor Home ([04-project-dialogs.md](../context/feature-specs/04-project-dialogs.md))
- Prisma Schema And Data Layer ([05-prisma.md](../context/feature-specs/05-prisma.md))
- Project REST API routes ([06-project-apis.md](../context/feature-specs/06-project-apis.md))
- Wired Editor Home & Project Dialogs to API ([07-wire-editor-home.md](../context/feature-specs/07-wire-editor-home.md))
- Built `/editor/[roomId]` Workspace Shell with Server access control and updated layout ([08-editor-workspace-shell.md](../context/feature-specs/08-editor-workspace-shell.md))
- Added Share Dialog UI and REST API collaborators endpoint ([09-share-dialog.md](../context/feature-specs/09-share-dialog.md))
- Collaborative Canvas Setup ([10-liveblocks-setup.md](../context/feature-specs/10-liveblocks-setup.md))
- Base Canvas Integration ([11-base-canvas.md](../context/feature-specs/11-base-canvas.md))
- Shape Panel UI & Drag-and-Drop Creation ([12-shape-panel.md](../context/feature-specs/12-shape-panel.md))
- Shape rendering and drag preview ([13-node-shape.md](../context/feature-specs/13-node-shape.md))
- Node resizing and inline label editing ([14-node-editing.md](../context/feature-specs/14-node-editing.md))
- Node Color Toolbar floating UI and Liveblocks synchronization ([15-node-color-toolbar.md](../context/feature-specs/15-node-color-toolbar.md))
- Custom edge behavior, connection handles, and inline label editing ([16-edge-behavior.md](../context/feature-specs/16-edge-behavior.md))
- Canvas zoom, fit view, and Liveblocks undo/redo history controls with global keyboard shortcuts ([17-canvas-ergonomics.md](../context/feature-specs/17-canvas-ergonomics.md))
- Starter Templates library and import modal ([18-starter-templates.md](../context/feature-specs/18-starter-templates.md))
- Presence, Avatars, and Cursors on the collaborative canvas ([19-presence-avatars-cursors.md](../context/feature-specs/19-presence-avatars-cursors.md))
- AI Sidebar shell component with tabs, message simulator, and mock spec generation ([20-ai-sidebar-shell.md](../context/feature-specs/20-ai-sidebar-shell.md))
- Canvas Autosave and Loading ([21-canvas-autosave.md](../context/feature-specs/21-canvas-autosave.md))
- Trigger.dev SDK installed and linked to cloud project `proj_luhcvxtdmhmmhucerfwj` ([22-trigger-dev-setup.md](../context/feature-specs/22-trigger-dev-setup.md))
- Design Agent API backend wiring: trigger route, task run tracking, and run-scoped tokens ([22-design-agent-api.md](../context/feature-specs/22-design-agent-api.md))
- Design Agent AI logic: Gemini-driven canvas generation with shared AI presence and status ([23-design-agent-logic.md](../context/feature-specs/23-design-agent-logic.md))
- Shared AI Presence & Status state: room-wide `ai-status-feed`, validated status payloads, thinking-aware cursors, and generation-aware sidebar ([24-ai-presence-state.md](../context/feature-specs/24-ai-presence-state.md))
- Collaborative sidebar chat over a separate room-scoped `ai-chat` feed, validated with a Zod schema, kept isolated from `ai-status-feed` ([25-sidebar-chat-feed.md](../context/feature-specs/25-sidebar-chat-feed.md))
- Functional AI chat: design prompts submit through the `ai-chat` feed, run status tracked in realtime via `useRealtimeRun`, status strip driven by `ai-status-feed`, AI canvas updates reflected automatically through Liveblocks ([26-ai-chat-functional.md](../context/feature-specs/26-ai-chat-functional.md))
- Spec Generation backend flow: `POST /api/ai/spec` trigger route, `POST /api/ai/spec/token` run-scoped token route, and the `generate-spec` Trigger.dev task producing a Markdown technical spec from the canvas + chat context ([27-spec-generation-flow.md](../context/feature-specs/27-spec-generation-flow.md))
- Spec persistence & secure download: `ProjectSpec` Prisma model (metadata only), spec content persisted to Vercel Blob at `specs/{projectId}/{specId}.md` inside the `generate-spec` task, and the access-checked `GET /api/projects/[projectId]/specs/[specId]/download` route returning the Markdown as an attachment ([28-spec-persistence-download.md](../context/feature-specs/28-spec-persistence-download.md))
- Spec UI integration: editor Specs tab now lists persisted `ProjectSpec` records for the project, opens a Markdown preview modal (content fetched on demand through the download endpoint), and downloads each spec via the browser ([29-spec-ui-integration.md](../context/feature-specs/29-spec-ui-integration.md))
- Performance investigation & fix: eliminated the per-request Clerk Backend API round-trip that was the shared bottleneck across the editor page, liveblocks-auth, canvas, and collaborators endpoints; parallelized independent awaits and removed a redundant Liveblocks round-trip ([30-fixing-performance-issue.md](../context/feature-specs/30-fixing-performance-issue.md))

## In Progress

- None.

## Next Up

- Phase 2: Collaborative Canvas Features & AI Generation

## Open Questions

- None.


- Fixed Trigger.dev **production deploy** failure ("DATABASE_URL environment variable is required" while importing `trigger/generate-spec.ts`). The `deploy` build indexes task files by importing them, but `lib/prisma.ts` threw at module-load time when `DATABASE_URL` was unset. Refactored `lib/prisma.ts` to a lazy `Proxy`-backed singleton: the client (and its `DATABASE_URL` check) is now created on first property access rather than at import, matching the existing lazy pattern in `lib/liveblocks.ts`. Kept the export effectively `any` to preserve prior delegate typing. Typecheck clean. Runtime still requires the Production env vars set in the Trigger.dev dashboard: `DATABASE_URL`, `LIVEBLOCKS_SECRET_KEY`, `GOOGLE_AI_API_KEY` (or `GOOGLE_GENERATIVE_AI_API_KEY`), `BLOB_READ_WRITE_TOKEN` (Vercel Blob), and optionally `GOOGLE_AI_MODEL`.

## Architecture Decisions

- Map shadcn/ui CSS variables to the specified custom dark theme tokens (`--bg-base`, etc.) to ensure automatic dark mode style synchronization without customizing the shadcn source code.
- Dynamically use `@prisma/adapter-ppg` (`PrismaPostgresAdapter`) when the `DATABASE_URL` host contains `.prisma.io` (Prisma Postgres databases) to connect via WebSockets/HTTPS on port 443, bypassing firewalls that block standard TCP port 5432.

## Session Notes

- Initiating setup of tailwind-merge, clsx, lucide-react, and shadcn/ui primitives.
- Setting up Clerk authentication, proxy.ts middleware, and custom dark auth pages.
- Fixed redirect loop and blank rendering screen issues by handling root `/` redirects and auth-page protection in `proxy.ts` (middleware/edge level) and setting `afterSignOutUrl="/sign-in"` on `ClerkProvider`.
- Refactored auth marketing panels into a shared `<AuthMarketingPanel />` component, implemented UUIDs for editor logs, pinned Clerk dependencies in the root `package.json`, set up a server-side route layout to protect `/editor`, and configured proxy middleware to return 401 for unauthorized API/tRPC requests.
- Implemented `/editor` home screen when no project is active, built ProjectDialogs for creating/renaming/deleting projects with slugify generator and loading/transition overlays, updated ProjectSidebar to handle project categorizations, hover rename/delete buttons, and mobile scrim backdrop closures. Fixed page height scrolling/layout shift bug by constraining main wrapper to `h-screen overflow-hidden` and added undefined array safety checks in sidebar filters.
- Implemented the database layer using Prisma v7 multi-file schema support. Created the `Project` and `ProjectCollaborator` models inside `prisma/models/project.prisma`, instantiated the cached client singleton `lib/prisma.ts` with direct/Accelerate connection branching, applied the initial database schema migration, and verified build compilation.
- Implemented the backend REST API routes (`GET /api/projects`, `POST /api/projects`, `PATCH /api/projects/[projectId]`, `DELETE /api/projects/[projectId]`) in Next.js 16. Added strict Clerk auth, project ownership verification checks, cascade delete for project collaborators on project deletion, and a shared `slugify` utility. Verified strict TypeScript type compliance and compiled build.
- Wired the editor home page, sidebar, and dialogs to the database REST API using a Server Component architecture. Extracted client layout elements into a dedicated `EditorClient` component, refactored `/editor` to fetch owned and shared projects server-side on load, created the dynamic `/editor/[projectId]` route to display individual workspaces after performing authentication checks, and implemented the `useProjectActions` mutation hook. Verified that the production build compiles successfully.
- Resolved Prisma connection timeout (`ETIMEDOUT`) and SSL warning issue in local development environment. By branching in `lib/prisma.ts` to instantiate `PrismaPostgresAdapter` from `@prisma/adapter-ppg` when connecting to `*.prisma.io` hosts, database connections are established over port 443 (HTTPS/WebSocket) instead of direct TCP port 5432.
- Created `lib/project-access.ts` containing Clerk identity extraction and workspace member/owner checking. Added centering dark layout for `AccessDenied` view when project lookup fails or rights are absent. Migrated routes to `/editor/[roomId]` dynamic segment using access control checks server-side. Refactored workspace shell in `EditorClient` to feature project header name, sharing action, toggleable right-hand AI assistant placeholder, and centered main canvas dot grid template.
- Hardened the design agent against provider quota failures (`trigger/design-agent.ts`). A test run failed with a Gemini `RESOURCE_EXHAUSTED` / `free_tier ... limit: 0` (429) error — a Google billing/quota issue, not a code bug (the key reached Gemini). Added `maxRetries: 1` to `generateObject` so the AI SDK stops retrying permanent 429s (was silently doing 3 attempts w/ backoff), an `isQuotaError()` classifier (`APICallError` statusCode 429 / quota / resource_exhausted) that broadcasts a distinct "out of API quota — check billing" status and returns `quotaExceeded`, and made the model configurable via `GOOGLE_AI_MODEL` (default `gemini-2.0-flash`). Build verified. Runtime unblock still requires enabling paid billing on the Gemini API key's project (or a free-tier-eligible key), and setting `GOOGLE_AI_API_KEY` in the Trigger.dev environment for deployed runs.
- Implemented `app/api/projects/[projectId]/collaborators` REST endpoints supporting collaborator lists (`GET`), invites (`POST`), and removals (`DELETE`). Integrated Clerk Backend API `getUserList()` to query display names and avatars from emails in bulk. Created `<ShareDialog />` allowing project link copies, new invites, and members list management, showing read-only states for collaborators. Wired the Share button inside the top navbar.
- Installed `@liveblocks/node` server-side dependencies. Configured Liveblocks `Presence` and `UserMeta` types in `liveblocks.config.ts`. Created cached server client in `lib/liveblocks.ts` with a deterministic color mapping helper. Implemented the dynamic `POST /api/liveblocks-auth` route handler to authorize collaborator room access, verifying Clerk session identity and project collaborator permissions.
- Created `types/canvas.ts` and implemented a Liveblocks-backed React Flow collaborative canvas. Configured `<CanvasWrapper />` with a custom class-based `ErrorBoundary`, `ClientSideSuspense` loading spinner, and error retry state, then mounted it inside `EditorClient` (replacing the static placeholder).
- Fixed editor canvas drag-and-drop pipeline and visual card/floating styling anomalies. Moved `onDragOver` and `onDrop` events directly onto the `<ReactFlow />` component, added explicit overrides in `globals.css` to force base background coloring and remove any borders, shadows, or border-radius.
- Refactored the right AI Assistant sidebar to be positioned as floating/fixed over the canvas, matching the left sidebar pattern, and supported smooth entrance/exit slide transitions with box shadow elevation and `inert` focus-trapping when closed. Adjusted the left sidebar translation offsets to completely eliminate peeking borders when collapsed.
- Replaced placeholder node shapes rendering with CSS-based rectangles, pills, and circles, and scaling SVG-based diamonds, hexagons, and cylinders. Designed specific stroke, color, and drop-shadow styling (glowing on selection) that adapts to each shape layout.
- Added a shape drag-and-drop preview system by generating dynamic, semi-transparent ghost elements and configuring them via the native HTML5 `setDragImage` event transfer interface during drag start. Verified compiled production build with full TypeScript checking.
- Implemented node resizing and inline label editing. Selected nodes render a custom dark-themed `NodeResizer` (using CSS variables `--accent-primary` and `--bg-base`). Double-clicking a node toggles interactive label editing inside a centered auto-growing `<textarea>` with full propagation controls to prevent canvas drag/pan and coordinate live syncing via Liveblocks.
- Implemented a floating color toolbar above selected nodes to update both background and text color pairs. Embedded the toolbar within the `CanvasNode` client component (visible when selected and not editing), configured it with 8 predefined theme colors matching `NODE_COLORS`, applied a custom scaling and glow effect on hover, and ensured interaction safety with `nodrag nopan nowheel` pointer event propagation blocks. Added updateColor callback mapping in `CollaborativeCanvas` to sync modifications with the collaborative canvas state instantly.
- Replaced default canvas edges with custom step-routed edges, styled connection handles on all 4 sides as subtle white circles with a dark border that fade in on hover, registered the custom `CanvasEdge` renderer with dynamic arrowhead selection (active vs dimmed), configured default edge options, and implemented inline label editing utilizing auto-growing text inputs inside React Flow's `EdgeLabelRenderer` with full event isolation.
- Implemented Canvas Ergonomics controls. Added a floating `CanvasControls` pill component at the bottom-left containing animated zoom, fit view, and Liveblocks undo/redo status-aware buttons. Created `hooks/useKeyboardShortcuts.ts` to bind global key event listeners for zoom and undo/redo operations with typing field validation.
- Implemented real-time collaborator presence indicators and live cursors on the editor canvas. Configured the Liveblocks Presence type in `liveblocks.config.ts` to track `thinking: boolean` and `cursor: { x: number; y: number } | null` states. Created the floating `<AvatarGroup />` overlay in the top-right corner of the canvas, which adjusts position dynamically when the AI assistant sidebar is opened/closed. The group displays an overlapping stack of up to 5 collaborator avatars (with profile photos or initials fallbacks and a +N overflow chip), a visual divider, and the Clerk `UserButton`. Conditionally hid the `UserButton` from the main `EditorNavbar` while inside a project canvas to keep layout ergonomics clean, and implemented a custom cursor renderer displaying a color-matched pointer and user name badge.
- Created the floating stateful AI Workspace Sidebar (`components/editor/ai-sidebar.tsx`) featuring AI Architect (chat interface with interactive starter chips and local simulation replies, auto-resizing input textarea) and Specs (generate spec button with dynamic simulator and card presentation) tabs. Integrated it into `app/editor/editor-client.tsx`, and configured scoped Tailwind styles inside `.theme-ai` block in `app/globals.css`.
- Implemented Canvas Autosave & Loading. Created dynamic GET/PUT endpoints at `app/api/projects/[projectId]/canvas/route.ts` to sync local canvas graph state with Vercel Blob and update the Prisma project record `canvasJsonPath`. Created a custom `useCanvasAutosave` hook to manage debounced PUT requests, and wired it into `CollaborativeCanvas` to handle loading the saved canvas state when the room is empty and syncing ongoing modifications. Added the Save button to the `EditorNavbar` with visual dynamic indicator states (Saving, Saved, Save Error).
- Resolved workspace canvas issues: wired the Save button on the workspace navbar to trigger manual saves and track statuses correctly by resolving state transitions in `useCanvasAutosave.ts` (using deferred timeouts to prevent React render-phase update warnings); fixed Vercel Blob access to `private` with SDK `get` streaming content download and configured `addRandomSuffix: true` on PUT requests to bypass overwrite errors in private stores. Added a keydown listener on window to handle collaborative deletions of selected nodes and edges via Delete/Backspace keys, ignoring inputs. Updated handles on custom nodes to render source/target pairs at all 4 sides, preserving original source handle IDs (`t`, `r`, `b`, `l`) to maintain compatibility with existing project and template edges. Standardized drag and drop offset calculations to center dropped elements exactly under the cursor. Disabled automatic fitView zoom on drops and added img.clerk.com to remotePatterns in next.config.ts.
- Installed and linked Trigger.dev (`@trigger.dev/sdk`, `@trigger.dev/build`) to the existing cloud project `proj_luhcvxtdmhmmhucerfwj` via `npx trigger.dev@latest init`. Relocated the generated task directory from the CLI's default `src/trigger` to a root-level `trigger/` folder to match this project's flat (non-`src`) layout and the `trigger` system boundary defined in `architecture-context.md`, updating `trigger.config.ts`'s `dirs` accordingly. Added a `TRIGGER_SECRET_KEY` placeholder to `.env` (to be filled in from the Trigger.dev dashboard) and a `trigger:dev` script to `package.json` for running the local dev worker. No actual background tasks (design/spec generation) implemented yet — this is infrastructure setup only.
- Implemented the Design Agent API backend flow (no AI logic yet). Added the `TaskRun` model (`prisma/models/task-run.prisma`) with a unique index on `runId` and a compound index on `userId`/`projectId`, applied via migration `20260701115726_add_task_run`. Created `POST /api/ai/design`, which authenticates the caller, verifies project access via `checkProjectAccess`, triggers `design-agent` through `tasks.trigger`, records a `TaskRun`, and returns the run ID. Created `POST /api/ai/design/token`, which looks up the `TaskRun` by `runId`, verifies the requesting user matches `taskRun.userId`, and returns a run-scoped read-only public token from `auth.createPublicToken`. Created `trigger/design-agent.ts` exporting a minimal `design-agent` task that logs the `{ prompt, roomId }` payload and echoes it back — no node/edge generation or AI provider calls. Verified with `npm run build`.
  - Note: when running Prisma CLI commands (`migrate dev`, `migrate diff`, etc.) on this project, do not pass an explicit `--schema` file path — it overrides the folder-based multi-file schema resolution configured in `prisma.config.ts` (`schema: "prisma/"`) and causes Prisma to see only `prisma/schema.prisma`, missing every model in `prisma/models/`. This nearly triggered a destructive migration that would have dropped the `Project`/`ProjectCollaborator` tables. Omit `--schema` (or point it at the `prisma/` directory) so all model files are included.
- Implemented the Design Agent AI logic. Rewrote `trigger/design-agent.ts` to interpret the user prompt with Gemini (`@ai-sdk/google` via `generateObject` + a Zod action schema) and apply the result to the shared canvas using the official backend flow utility `mutateFlow` from `@liveblocks/react-flow/node` (add/move/resize/updateData/delete node, add/delete edge — cascading edge removal on node delete, palette/shape/spacing constraints baked into the system prompt). The task reads current room state first, then applies mutations, and broadcasts `AI_STATUS` (start → processing → complete/error) and `AI_PRESENCE` (thinking ghost cursor) Liveblocks room events so **all** participants see the same AI activity, while also mirroring progress into Trigger.dev run `metadata` for the initiating user. Added the `RoomEvent` union to `liveblocks.config.ts`, a new `components/editor/ai-activity-layer.tsx` (renders the AI ghost cursor + shared status feed, driven by `useEventListener` + `flowToScreenPosition`) mounted in `CollaborativeCanvas`, and replaced the AI sidebar's mock chat with a real flow: it POSTs to `/api/ai/design`, exchanges the run id for a run-scoped token via `/api/ai/design/token`, and subscribes with `useRealtimeRun` (headless `DesignRunWatcher`) to stream status and surface the final summary. Errors are caught inside the task (returns `{ ok: false }`, broadcasts an error status, always clears AI presence in `finally`) so a failed generation never corrupts the canvas.
  - Note: the repo had a broken AI SDK version pairing — `@ai-sdk/google@^4` (model spec `v4`) is a major ahead of what the installed `ai@6.0.217` speaks (`@ai-sdk/provider@3`, spec `v2/v3`), which fails both typecheck and runtime. Pinned `@ai-sdk/google` to `3.0.87`, which resolves to the exact `@ai-sdk/provider@3.0.13` / `@ai-sdk/provider-utils@4.0.34` that `ai@6.0.217` bundles. If `ai` is ever upgraded to a `v4`-spec line, bump `@ai-sdk/google` back to `^4` to match.
  - Note: AI presence is delivered via Liveblocks `broadcastEvent` (not per-connection Presence) because the agent runs in a Trigger.dev worker, not a connected browser client; broadcasting reaches every room participant and is rendered ephemerally by `AiActivityLayer`.
- Resolved `@clerk/ui` stylesheet build resolution failure in `app/globals.css` by pointing the import path directly to `../node_modules/@clerk/ui/dist/themes/shadcn.css`. Verified compile and build passes successfully.
- Implemented Shared AI Presence & Status state (spec 24). Added `types/tasks.ts` defining the `ai-status-feed` payload contract — a Zod `aiStatusMessageSchema` (`phase` enum + optional `text`), `parseAiStatusMessage` validator, and `isActivePhase`/`defaultStatusText` helpers — kept generic so design and later spec generation can share the feed. Realigned the `AI_STATUS` Liveblocks room event to that schema (`phase` + optional `text`, was `message`) in `liveblocks.config.ts`, updated the design agent's `publishStatus` broadcast to emit `text`, and made `AiActivityLayer` validate incoming payloads via `parseAiStatusMessage` (with `defaultStatusText` fallback). Live cursors (`CustomCursor` in `collaborative-canvas.tsx`) now read `thinking` from each collaborator's presence and render a spinner in the name badge. The AI sidebar now subscribes to the shared `ai-status-feed` via `useEventListener`, shows the most recent validated status (header "Working" chip + in-list bubble), disables the chat input and shows a spinner on the send button while generation is active anywhere in the room, and sets its own presence `thinking` while its run is pending so other participants' cursors reflect it. Dropped the redundant Trigger.dev metadata→status forwarding in `DesignRunWatcher` (interim status now flows through the Liveblocks feed; the watcher only finalizes summary/error). Verified `npm run build`.
  - Architecture note: the `LiveblocksProvider`/`RoomProvider` were lifted out of `CanvasWrapper` into a new `WorkspaceRoom` provider (in `canvas-wrapper.tsx`) that wraps BOTH the canvas and the `AiSidebar` in `editor-client.tsx`. Previously the sidebar sat outside the room and could not use Liveblocks hooks; sharing one room connection lets the sidebar subscribe to the feed and read/write presence without a parallel realtime channel (also unblocks the `ai-chat` feed in spec 25). `CanvasWrapper` now only owns the `ErrorBoundary`/`ClientSideSuspense`/`ReactFlowProvider` around the canvas.
- Implemented the functional AI chat (spec 26). The AI sidebar's design conversation now flows through the shared, room-scoped `ai-chat` feed instead of local-only component state, so it is collaborative across every session. `AiSidebar`'s `handleSend` publishes the user's design prompt to `ai-chat` (broadcast + local mirror via a new `publishChat` helper), then triggers the durable design agent (`POST /api/ai/design` → `{ runId }`, `POST /api/ai/design/token` → run-scoped `publicToken`), stores both in local `activeRun` state, and subscribes with `useRealtimeRun(runId, { accessToken })` through the headless `DesignRunWatcher`. While a run is active anywhere in the room (derived from `activeRun` **or** the shared `ai-status-feed`), the Architect input is disabled and the send button shows a spinner; on terminal status the watcher publishes the final AI summary — or, on failure/transport error, the error text — to `ai-chat` as an `assistant`-role message (per "show errors as messages in the `ai-chat` feed"). Added a compact **status strip** above the Architect input, shown only during active runs, that reflects the latest validated `ai-status-feed` message (dark `bg-elevated` base, green accent, animated ping dot). Canvas nodes/edges/presence are **not** touched by the sidebar — those changes arrive automatically through Liveblocks (`useLiveblocksFlow`) driven by the agent's backend `mutateFlow`. Introduced a single `--accent-green: #62c073` design token (mapped to `--color-accent-green`) in `globals.css` — user chat bubbles and both submit buttons now use `bg-accent-green` with dark (`--bg-base`) ink for readable contrast, replacing the previous cyan `brand` styling. A shared module-level `ChatBubble` component renders every `ai-chat` entry (assistant → dark AI bubble left; own user message → green right; others → dark left with sender name) and is reused by both the Architect and Team Chat tabs, since both now render the one shared `ai-chat` conversation. Verified `npm run build`.
  - Decision: routed the Architect design conversation through the existing `ai-chat` feed (rather than a new channel) because spec 26 step 1 requires the submit action to push the user message to `ai-chat`, and the check items require chat updates to appear across sessions. The Team Chat tab from spec 25 was kept (per "do not redesign the sidebar / keep current layout intact"); both tabs now share the same `ai-chat`-backed message list, so the design prompts, AI replies, and free-form team messages are one collaborative conversation viewable from either tab — only the Architect input triggers the agent. The `assistant` role was already reserved in `aiChatMessageSchema` (spec 25), so adding AI replies needed no schema change.
  - Decision: kept the existing two-call token acquisition (`/api/ai/design` then `/api/ai/design/token`) instead of merging the `publicToken` into the `/api/ai/design` response. Spec 26's scope limit "do not implement backend or Trigger.dev logic" outranks the step-1 phrasing "read `{ runId, publicToken }` from the response"; the two-call client flow already yields the same `runId` + run-scoped token and satisfies every check-when-done item without any backend edit.
- Implemented the Spec Generation backend flow (spec 27), mirroring the design-agent trigger/token/run-ownership pattern. Created `POST /api/ai/spec`: authenticates via Clerk, validates the body with Zod (`roomId`, `chatHistory`, `nodes`, `edges` — deliberately **no** client `projectId`), resolves project access with `checkProjectAccess(roomId, …)` since the Liveblocks room id **is** the project id, triggers `generate-spec` with the server-resolved `projectId` (`access.project.id`), records a `TaskRun` for the caller, and returns `{ runId }`. Created `POST /api/ai/spec/token`: authenticates, verifies the `TaskRun` belongs to the requester (`taskRun.userId === identity.userId`), and issues a run-scoped read-only `auth.createPublicToken` with `expirationTime: "1h"`. Created `trigger/generate-spec.ts` (`generateSpecTask`, id `generate-spec`): accepts a loose input at the trigger boundary and validates it with Zod inside `run` (lenient `.passthrough()` node/edge/chat schemas since they're JSON-serialized client graph data), flattens the canvas + chat into prompt context, and calls Gemini via `@ai-sdk/google` (`createGoogleGenerativeAI` + `generateText`, model from `GOOGLE_AI_MODEL` default `gemini-2.0-flash`, `GOOGLE_AI_API_KEY`/`GOOGLE_GENERATIVE_AI_API_KEY`) to produce a Markdown technical spec (Overview/Architecture/Components/Data Flow/Technical Requirements/Open Questions). Status is tracked for realtime via Trigger.dev run `metadata` (`phase`/`text`/`progress`), and quota (429) failures are classified with the same `isQuotaError` helper and surfaced distinctly; the run returns `{ ok, spec }` (or `{ ok: false, error, quotaExceeded }`) as plain Markdown output. `retry.maxAttempts: 1` (spec gen is read-only/idempotent, but 429s are permanent so extra retries only burn compute). TypeScript + `npm run build` pass; both `/api/ai/spec` and `/api/ai/spec/token` are registered routes.
  - Decision: used **Gemini via `@ai-sdk/google`** (per spec 27's explicit requirement) even though the design agent was later switched to OpenRouter over Gemini free-tier quota. `@ai-sdk/google@3.0.87` is already installed and provider-compatible with `ai@6`, so no new provider abstraction was added (respecting the scope limit). If the same 429/free-tier quota issue that hit the design agent recurs here, enable paid Gemini billing or set `GOOGLE_AI_MODEL`/`GOOGLE_AI_API_KEY` in the Trigger.dev environment. Per scope limits, this unit adds **no** frontend/spec-editor UI and does **not** persist the generated spec (blob/DB storage is a later unit) — the Markdown is returned as task output only.
  - Decision: validated the canvas graph/chat with `.passthrough()` Zod schemas (not the strict `canvasNode`/`canvasEdge` types) because React Flow nodes carry client-only callbacks (`updateLabel`, `updateColor`) that are stripped on JSON serialization and extra fields we don't read; passthrough keeps the payload robust while still requiring the fields the prompt builder uses.
- Implemented Spec Persistence & Secure Download (spec 28), reusing the canvas persistence separation (Postgres metadata + Vercel Blob content). Added the `ProjectSpec` Prisma model (`id`, `projectId` → `Project` relation with `onDelete: Cascade`, `filePath`, `createdAt`, indexed on `[projectId, createdAt]`) plus a `specs ProjectSpec[]` back-relation on `Project`, applied via migration `20260703132240_add_project_spec` (ran `prisma migrate dev` **without** `--schema` per the multi-file schema note). Persistence happens inside the `generate-spec` Trigger.dev task (not a request handler, since generation is long-lived background work): after the Gemini call returns the Markdown, it mints a `specId` (`randomUUID()`) up front to key the deterministic blob path `specs/{projectId}/{specId}.md`, uploads the content to a **private** Vercel Blob (`put`, `contentType: "text/markdown"`), then writes a `ProjectSpec` row storing `blob.url` in `filePath` (metadata only — no spec content in Postgres). The task now returns `{ ok, specId, spec }` and emits an interim "saving" status. Created the secure download route `GET /api/projects/[projectId]/specs/[specId]/download`: authenticates via Clerk, verifies project access with `checkProjectAccess`, loads the `ProjectSpec` and confirms `spec.projectId === projectId` (404 otherwise), fetches the content server-side from the private blob store via the SDK `get` stream (the Blob URL is never exposed to the client), and returns it as a `text/markdown` attachment (`Content-Disposition: attachment; filename="spec-{specId}.md"`) with proper 401/403/404/500 handling. No frontend/UI changes and no changes to existing canvas persistence. `npx tsc --noEmit` passes clean; `npm run build` fails only on the pre-existing Google Fonts (Geist) network fetch, unreachable in the sandbox — unrelated to these changes.
- Implemented Spec UI integration (spec 29). The editor's AI sidebar **Specs** tab now surfaces real persisted specs instead of the mock/demo cards. Added a new `components/editor/spec-list.tsx` (`<SpecList projectId refreshKey />`) that: fetches spec metadata for the current project, renders a compact, scrollable list (filename + `createdAt`, each item clickable), opens a shadcn `Dialog` preview modal on selection, and provides a download action on both the list item and the modal. The preview fetches the Markdown **through the existing `GET /api/projects/[projectId]/specs/[specId]/download` endpoint** (read as text) — the private Blob URL is never touched from the client — and content is only held while the modal is open (a nested `SpecPreview` mounts on open, drops the text on close), satisfying "do not store spec content in frontend state long-term". Rendering uses a small **dependency-free Markdown renderer** (headings, ordered/unordered lists, fenced code, blockquotes, rules, paragraphs with inline bold/code/italic) since no `react-markdown` dependency exists and the offline env can't reliably add one; it stays within the shadcn `Dialog`/`ScrollArea`/`Button` + existing color-token constraints. Download uses a programmatic same-origin anchor to the download route, letting the browser handle the file (the route already sends `Content-Disposition: attachment`). The sidebar's existing "Generate Spec" button was kept (no redesign) but now bumps a `specRefreshKey` on completion to re-fetch the list rather than showing a mock card. `npx tsc --noEmit` and `npm run build` both pass; the new route appears in the build's route table.
  - Follow-up: wired the sidebar's "Generate Spec" button to the real backend (it was still the mock `setTimeout` from spec 20, so no `ProjectSpec` was ever created). `handleGenerateSpec` now reads the current canvas from `GET /api/projects/[projectId]/canvas` (last autosaved state — the sidebar sits outside the React Flow provider) plus the local `ai-chat` history, POSTs `/api/ai/spec` → `{ runId }`, exchanges it at `/api/ai/spec/token` → run-scoped token, and hands off to a new headless `SpecRunWatcher` (`useRealtimeRun<typeof generateSpecTask>`). On the terminal `COMPLETED`/`ok` status it bumps `specRefreshKey` so `SpecList` re-fetches and the new spec appears; failures (incl. quota 429) surface in an inline error row. The button spins for the full run duration now (no fixed 1.5s). Requires the Trigger.dev worker (`npm run trigger:dev`) running and a valid `GOOGLE_AI_API_KEY`, otherwise the run never processes.
  - Fix: the Prisma client at `app/generated/prisma` predated the `ProjectSpec` model (spec 28 added the model but never regenerated the client), so `prisma.projectSpec` was `undefined` at runtime — breaking both the specs list route and the `generate-spec` task's `.create`. Ran `npx prisma generate` (schema/table already migrated). Also note: running `next build` while `next dev` is live corrupts the shared `.next` dir and causes phantom 404s on newly added routes — clear `.next` and restart dev after a stray build.
  - Decision: added a thin read-only `GET /api/projects/[projectId]/specs` route (auth + `checkProjectAccess`, returns `{ id, createdAt, filename: "spec-{id}.md" }[]`, metadata only). Spec 29 says "fetch specs using the existing ProjectSpec API" and "do not implement backend logic", but only the `/download` route existed — there was no list endpoint, so the list could not load without one. Interpreted "do not implement backend logic" as the generation/persistence pipeline (specs 27/28); a metadata-only list read following the established route pattern is the minimum needed to make the tab function, so I added it. The derived `filename` matches the download route's `Content-Disposition` (`spec-{id}.md`) since `ProjectSpec` stores no filename field.
- Investigated and fixed the app-wide performance issue (spec 30). Ruled out the DB-config hypotheses against the code first: `lib/prisma.ts` is already a proper `globalThis` singleton (H1), the `*.prisma.io` adapter routes over pooled WebSocket/443 (H2), and the queried columns are already indexed (`Project.id` PK, `ProjectCollaborator @@index([email])` + `@@unique([projectId, email])`, so access reads are index-covered — H4). Found the Clerk anti-pattern (H8): every request made one or more synchronous Clerk Backend API round-trips (`clerkClient().users.getUser()` / `currentUser()`) purely to read the user's email list, even though `userId` is free from the session JWT.
  - **Then added `performance.now()` instrumentation (`lib/perf.ts`, per spec step 4) and measured locally — the numbers corrected the diagnosis.** The Clerk cache brought auth to ~18ms, but the **dominant cost is the DB round-trip itself: a single PK `findUnique` (even a raw `SELECT 1`) against `pooled.db.prisma.io` takes ~1.5–3.5s** from this environment. Because one editor load fires ~4 requests that each re-run the *same* `checkProjectAccess` query, the app paid that ~3s round-trip 4× per load. Measured canvas GET breakdown: cold `auth+access=3526ms blob-read=772ms total=4299ms` → **warm `auth+access=0.1ms blob-read=100–220ms total=100–220ms`** after caching (~25–40× faster on the access path).
  - Fixes applied: (1) `getCachedClerkUser(userId)` — short-TTL (60s) `globalThis` cache; routed every user lookup through it (`getClerkIdentity`, `/api/projects` GET, `/editor` page, collaborators). (2) **`getCachedProject(projectId)` + `invalidateProjectCache(projectId)` in `lib/project-access.ts`** — 10s `globalThis` cache of the project record (with collaborators), keyed by projectId and shared across requests; `checkProjectAccess` and the collaborators GET read through it. This is the big win — it collapses the ~4 redundant ~3s access-check round-trips per page load into one. Invalidation is wired into every mutation: canvas PUT (`canvasJsonPath` change), collaborator POST/DELETE, and project PATCH/DELETE; the short TTL self-heals anything an invalidation misses (e.g. a mutation on another serverless instance, where Liveblocks already covers canvas realtime). Access is still evaluated per-request against the caller's identity, so it stays correct. (3) Parallelized independent awaits (H5): `[roomId]` page runs `checkProjectAccess` + `fetchProjectsForUser` via `Promise.all`; collaborators GET fires owner + current-user + bulk `getUserList` concurrently. (4) Removed the redundant `getOrCreateRoom` round-trip from `liveblocks-auth`. (5) `lib/perf.ts` timing logs (on by default in dev, `PERF_LOG=1` in prod) + `PRISMA_LOG=1`-gated query logging.
  - **Root cure is infra, not code**: the ~3s base per-query latency is the remote `pooled.db.prisma.io` WebSocket round-trip. The code already supports Prisma Accelerate (`createAccelerateClient` + `withAccelerate`), which adds a global pool + edge read-caching over 443 — switching `DATABASE_URL` to the `prisma+postgres://…` Accelerate URL would cut the base latency for *all* queries (and also bypasses the 5432 firewall, so it's strictly better than the current ppg adapter for local + prod). This requires the account's Accelerate API key, so it's left for the user to set. The caching above makes the app fast today regardless; Accelerate would additionally speed up the unavoidable cold reads. `npx tsc --noEmit` passes; verified live against the running dev server (cold vs. warm numbers above). Future code optimization: read verified emails from Clerk session-JWT claims to drop the first getUser entirely (needs a JWT template change, deferred).
- Implemented collaborative sidebar chat over a separate `ai-chat` feed (spec 25). Extended `types/tasks.ts` with the chat contract — `AI_CHAT_FEED` constant, `aiChatRoleSchema` (`user`/`assistant`, only `user` emitted today), an `aiChatMessageSchema` (`id`, `sender`, `role`, `content`, `timestamp`), and a `parseAiChatMessage` validator — following the same shape/validator pattern as the existing `ai-status-feed` contract. Added an `AI_CHAT` variant to the `RoomEvent` union in `liveblocks.config.ts` carrying a nested `message: AiChatMessage`, kept deliberately distinct from `AI_STATUS` so chat is never mixed with status. Wired a new **"Team Chat"** tab into `ai-sidebar.tsx` (tabs went `grid-cols-2` → `grid-cols-3`: Architect / Chat / Specs): it subscribes to `ai-chat` via a dedicated `useEventListener` (validating every incoming payload with `parseAiChatMessage` before rendering), renders messages in order with sender/timestamp/content reusing the existing bubble styling, and sends via `useBroadcastEvent` — deriving the sender name from `useSelf().info.name`, validating its own outgoing payload, mirroring the message locally (broadcastEvent does not echo to the sender), clearing the input on success, and surfacing a small inline error row on failure (e.g. not connected to the room). A dedicated **Team Chat tab** was chosen rather than reusing the Architect input because the Architect input triggers the design agent, which spec 25's scope limits forbid — this keeps the chat human-to-human only, with no AI replies and no backend tasks. Verified `npm run build`.

